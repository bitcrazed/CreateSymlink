<Page
    x:Class="CreateSymlink.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CreateSymlink"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition MinHeight="200" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
        </StackPanel>
        <StackPanel Grid.Row="1">
            <Button 
                x:Name="CreateSymlinkButton" 
                HorizontalAlignment="Center" 
                Click="CreateSymlinkButton_Click">
                Create Symlink
            </Button>
            <TextBlock 
                x:Name="CreateSymlinksResultMessageBox" 
                HorizontalAlignment="Center" 
                Margin="20 20"
                TextWrapping="WrapWholeWords"
                Text="Press the button above to create a Symlink">
            </TextBlock>
        </StackPanel>
    </Grid>
</Page>
